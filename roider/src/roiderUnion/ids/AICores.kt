package roiderUnion.ids

import com.fs.starfarer.api.impl.campaign.ids.Commodities

enum class AICores(val id: String) {
    ALPHA(Commodities.ALPHA_CORE),
    BETA(Commodities.BETA_CORE),
    GAMMA(Commodities.GAMMA_CORE);
}